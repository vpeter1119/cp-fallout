/* export class Dice {
    sides: number;
    roll: function (rolls: number[] = []): number[] {
      const roll = this.randomIntFromInterval(1, 10);
      rolls.push(roll);
      if (roll == 10)
      {
        this.d10.roll(rolls);
      }
      else
      {
        return rolls;
      }
    }
}
 */